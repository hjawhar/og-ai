# dependencies (bun.lock IS committed on purpose — reproducible installs)
node_modules/

# the Angular workspace's build output and CLI cache
ui/dist/
ui/.angular/

# a partial download is not usable weights
*.gguf.part

# logs, including the sweep driver's server capture
*.log

# model weights: never commit multi-GiB GGUFs
*.gguf

# downloaded release archives and unpacked toolkits, if anyone stages them here
*.zip
*.tar.gz
*.deb

# OS cruft
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store
